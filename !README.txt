To run the front-end:
npm install --legacy-peer-deps
npm start
(REQUIRES NodeJS 12.8.0)

To run the back-end:
Create SQL database called "pharma_app"
Check that application.properties credentials match